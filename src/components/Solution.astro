<section class="solution" id="solution">
  <div class="section-container">
    <div class="solution-layout">
      <div class="solution-content">
        <span class="section-badge">Solusinya</span>
        <h2 class="section-title" style="text-align:left">Cukup <span class="gradient-text">chat.</span></h2>
        <p class="solution-text">
          Aidah adalah asisten keuangan AI yang paham bahasa sehari-hari kamu.
        </p>
        <p class="solution-text">
          Ketik <strong>"makan siang 35rb"</strong> — otomatis tercatat, terkategorikan, dan masuk laporan.
        </p>
        <p class="solution-text solution-highlight">
          Tanpa form. Tanpa spreadsheet. Tanpa ribet.
        </p>
        <a href="#how-it-works" class="btn-primary btn-lg">
          <span>Lihat Cara Kerjanya</span>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
      </div>
      <div class="solution-visual">
        <div class="solution-card">
          <div class="sol-chat-row sol-user">
            <div class="sol-bubble sol-bubble-user">makan siang 35rb</div>
          </div>
          <div class="sol-chat-row sol-ai">
            <div class="sol-avatar">🐠</div>
            <div class="sol-bubble sol-bubble-ai">
              <span class="sol-tag sol-tag-expense">🛒 Pengeluaran</span>
              <div class="sol-detail"><span>Kategori</span><strong>Makanan</strong></div>
              <div class="sol-detail"><span>Nominal</span><strong>Rp 35.000</strong></div>
              Tercatat! Sisa budget makan: Rp 465.000 ✅
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .solution{padding:80px 0;background:var(--bg)}
  .solution-layout{display:grid;grid-template-columns:1fr 1fr;gap:48px;align-items:center}
  .solution-text{font-size:16px;color:var(--muted);line-height:1.7;margin-bottom:12px;font-weight:600}
  .solution-text strong{color:var(--text);font-weight:800}
  .solution-highlight{color:var(--blue-600);font-weight:800;font-size:17px;margin-bottom:24px}
  .solution-card{background:var(--surface);border:2.5px solid var(--ink);border-radius:var(--radius-lg);padding:24px;box-shadow:6px 6px 0 var(--ink);display:flex;flex-direction:column;gap:14px}
  .sol-chat-row{display:flex;gap:8px}
  .sol-user{justify-content:flex-end}
  .sol-ai{align-items:flex-start}
  .sol-avatar{width:32px;height:32px;border-radius:50%;background:var(--blue-500);display:flex;align-items:center;justify-content:center;font-size:14px;flex-shrink:0;border:2px solid var(--blue-200)}
  .sol-bubble{padding:12px 16px;border-radius:18px;font-size:14px;line-height:1.6;font-weight:600;max-width:85%}
  .sol-bubble-user{background:var(--blue-500);color:#fff;border-bottom-right-radius:5px}
  .sol-bubble-ai{background:var(--blue-50);border:2px solid var(--ink);color:var(--text);border-bottom-left-radius:5px;box-shadow:2px 2px 0 var(--ink)}
  .sol-tag{display:inline-flex;font-size:11px;font-weight:800;padding:3px 10px;border-radius:99px;margin-bottom:8px}
  .sol-tag-expense{background:var(--expense-bg);color:var(--expense-text)}
  .sol-detail{display:flex;justify-content:space-between;align-items:center;padding:2px 0;font-size:13px}
  .sol-detail span{color:var(--muted);font-weight:600}
  .sol-detail strong{color:var(--text);font-weight:800}
  @media(max-width:768px){
    .solution-layout{grid-template-columns:1fr;text-align:center}
    .solution-layout .section-title{text-align:center!important}
    .solution-layout .btn-primary{margin:0 auto}
    .solution-visual{max-width:400px;margin:0 auto}
  }
</style>
