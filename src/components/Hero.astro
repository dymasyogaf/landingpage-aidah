<section class="hero" id="hero">
  <div class="hero-bg">
    <div class="hero-orb hero-orb-1"></div>
    <div class="hero-orb hero-orb-2"></div>
    <div class="hero-orb hero-orb-3"></div>
  </div>
  <div class="hero-container">
    <div>
      <div class="hero-badge">
        <span class="badge-dot"></span>
        <span>Powered by AI — Gratis untuk semua</span>
      </div>
      <h1 class="hero-title">
        Permudah <span class="gradient-text">Keuanganmu</span><br />
        dengan Kekuatan AI
      </h1>
      <p class="hero-subtitle">
        Cukup ketik seperti ngobrol — <strong>"beli kopi 25rb"</strong> atau <strong>"tabung 10% dari gaji"</strong>.
        Aidah otomatis mencatat, mengkategorikan, dan menganalisis keuanganmu.
      </p>
      <div class="hero-actions">
        <a href="https://app.aidah.web.id/register" class="btn-primary btn-lg">
          <span>Mulai Gratis</span>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
        <a href="#how-it-works" class="btn-secondary btn-lg">
          <span>Lihat Demo</span>
        </a>
      </div>
      <div class="hero-stats">
        <div class="stat-item"><span class="stat-number">100%</span><span class="stat-label">Gratis</span></div>
        <div class="stat-divider"></div>
        <div class="stat-item"><span class="stat-number">AI</span><span class="stat-label">Otomatis Parsing</span></div>
        <div class="stat-divider"></div>
        <div class="stat-item"><span class="stat-number">🔒</span><span class="stat-label">Data Terenkripsi</span></div>
      </div>
    </div>
    <div class="hero-preview">
      <div class="preview-window">
        <div class="preview-header">
          <div class="preview-dots"><span></span><span></span><span></span></div>
          <span class="preview-title">Aidah Chat</span>
        </div>
        <div class="preview-chat">
          <div class="chat-msg chat-user"><div class="msg-bubble msg-user-bubble">gaji masuk 8jt</div></div>
          <div class="chat-msg chat-ai">
            <div class="ai-avatar-sm">🐠</div>
            <div class="msg-bubble msg-ai-bubble">
              <div class="msg-parsed">
                <span class="parsed-badge parsed-income">💰 Pemasukan</span>
                <div class="parsed-detail"><span class="parsed-label">Nominal</span><span class="parsed-value">Rp 8.000.000</span></div>
                <div class="parsed-detail"><span class="parsed-label">Kategori</span><span class="parsed-value">Gaji</span></div>
              </div>
              Tercatat! Saldo bulan ini +Rp 8.000.000 🎉
            </div>
          </div>
          <div class="chat-msg chat-user"><div class="msg-bubble msg-user-bubble">nabung 15% dari gaji</div></div>
          <div class="chat-msg chat-ai">
            <div class="ai-avatar-sm">🐠</div>
            <div class="msg-bubble msg-ai-bubble">
              Budget <strong>Tabungan</strong> dibuat: <strong>Rp 1.200.000</strong>/bulan (15% × 8jt) ✅
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const chatMessages = document.querySelectorAll('.preview-chat .chat-msg');
  chatMessages.forEach((msg, i) => {
    (msg as HTMLElement).style.animationDelay = `${i * 0.8}s`;
  });
</script>

<style>
  .hero{position:relative;min-height:100vh;display:flex;align-items:center;padding:110px 0 60px;overflow:hidden;background:var(--blue-500)}
  .hero-bg{position:absolute;inset:0;pointer-events:none;overflow:hidden}
  .hero-orb{position:absolute;border-radius:50%;opacity:0.25;animation:float 10s ease-in-out infinite}
  .hero-orb-1{width:400px;height:400px;background:#93C5FD;top:-10%;left:-8%;animation-delay:0s}
  .hero-orb-2{width:300px;height:300px;background:#BFDBFE;top:30%;right:-5%;animation-delay:-3s}
  .hero-orb-3{width:250px;height:250px;background:#60A5FA;bottom:-5%;left:25%;animation-delay:-6s}
  @keyframes float{0%,100%{transform:translate(0,0)}50%{transform:translate(20px,-20px)}}
  .hero::before{content:'';position:absolute;bottom:0;left:0;right:0;height:80px;background:var(--bg);border-radius:40px 40px 0 0;z-index:3}
  .hero-container{max-width:1100px;margin:0 auto;padding:0 24px;display:grid;grid-template-columns:1fr 1fr;gap:50px;align-items:center;position:relative;z-index:2}
  .hero-badge{display:inline-flex;align-items:center;gap:8px;background:rgba(255,255,255,0.2);border:1.5px solid rgba(255,255,255,0.35);border-radius:99px;padding:6px 16px 6px 10px;font-size:13px;font-weight:700;color:#fff;margin-bottom:20px;width:fit-content}
  .badge-dot{width:8px;height:8px;border-radius:50%;background:#DCFCE7;animation:pulse 2s infinite}
  @keyframes pulse{0%,100%{opacity:1}50%{opacity:0.4}}
  .hero-title{font-size:clamp(32px,5vw,52px);font-weight:900;line-height:1.1;letter-spacing:-1px;margin-bottom:16px;color:#fff}
  .hero-title .gradient-text{color:#fff;text-shadow:0 2px 8px rgba(0,0,0,0.1)}
  .hero-subtitle{font-size:16px;color:var(--blue-100);line-height:1.7;max-width:480px;margin-bottom:28px}
  .hero-subtitle strong{color:#fff;font-weight:800}
  .hero-actions{display:flex;gap:12px;margin-bottom:36px;flex-wrap:wrap}
  .hero .btn-primary{background:#fff;color:var(--blue-600);border-color:var(--ink)}
  .hero .btn-primary svg{stroke:var(--blue-600)}
  .hero .btn-secondary{background:rgba(255,255,255,0.15);color:#fff;border-color:rgba(255,255,255,0.4);box-shadow:none}
  .hero .btn-secondary:hover{background:rgba(255,255,255,0.25)}
  .hero-stats{display:flex;align-items:center;gap:20px}
  .stat-item{display:flex;flex-direction:column;align-items:center}
  .stat-number{font-size:22px;font-weight:900;color:#fff}
  .stat-label{font-size:11px;color:var(--blue-200);font-weight:700;margin-top:2px}
  .stat-divider{width:1px;height:32px;background:rgba(255,255,255,0.25)}
  .hero-preview{position:relative}
  .preview-window{background:var(--surface);border:2.5px solid var(--ink);border-radius:var(--radius-lg);overflow:hidden;box-shadow:6px 6px 0 var(--ink);animation:slideUp 0.6s ease-out}
  @keyframes slideUp{from{opacity:0;transform:translateY(30px)}to{opacity:1;transform:translateY(0)}}
  .preview-header{display:flex;align-items:center;gap:10px;padding:12px 16px;background:var(--blue-50);border-bottom:2px solid var(--ink)}
  .preview-dots{display:flex;gap:6px}
  .preview-dots span{width:11px;height:11px;border-radius:50%;border:1.5px solid var(--ink)}
  .preview-dots span:first-child{background:#F87171}
  .preview-dots span:nth-child(2){background:#FBBF24}
  .preview-dots span:last-child{background:#4ADE80}
  .preview-title{font-size:13px;font-weight:800;color:var(--text);margin-left:auto;margin-right:auto}
  .preview-chat{padding:16px;display:flex;flex-direction:column;gap:12px;background:var(--blue-50)}
  .chat-msg{display:flex;gap:8px;animation:msgIn 0.5s ease-out both}
  .chat-user{justify-content:flex-end}
  .chat-ai{align-items:flex-start}
  @keyframes msgIn{from{opacity:0;transform:translateY(8px)}}
  .msg-bubble{padding:10px 14px;border-radius:18px;font-size:13px;line-height:1.5;font-weight:600;max-width:85%}
  .msg-user-bubble{background:var(--blue-500);color:#fff;border-bottom-right-radius:5px}
  .msg-ai-bubble{background:#fff;border:2px solid var(--ink);color:var(--text);border-bottom-left-radius:5px;box-shadow:2px 2px 0 var(--ink)}
  .ai-avatar-sm{width:28px;height:28px;border-radius:50%;background:var(--blue-500);display:flex;align-items:center;justify-content:center;font-size:13px;flex-shrink:0;margin-top:2px;border:2px solid var(--blue-200);color:#fff}
  .msg-parsed{background:var(--blue-50);border:1.5px solid var(--border);border-radius:12px;padding:10px 12px;margin-bottom:8px}
  .parsed-badge{display:inline-flex;font-size:11px;font-weight:800;padding:3px 10px;border-radius:99px;margin-bottom:6px}
  .parsed-income{background:var(--income-bg);color:var(--income-text)}
  .parsed-detail{display:flex;justify-content:space-between;align-items:center;padding:3px 0}
  .parsed-label{font-size:11px;color:var(--muted);font-weight:600}
  .parsed-value{font-size:13px;font-weight:800;color:var(--text)}
  @media(max-width:1024px){
    .hero-container{grid-template-columns:1fr;text-align:center;gap:36px}
    .hero-subtitle{margin-left:auto;margin-right:auto}
    .hero-actions{justify-content:center}
    .hero-stats{justify-content:center}
    .hero-preview{max-width:420px;margin:0 auto}
  }
  @media(max-width:768px){.hero{padding:100px 0 80px}}
  @media(max-width:480px){
    .hero-stats{flex-direction:column;gap:10px}
    .stat-divider{width:40px;height:1px;background:rgba(255,255,255,0.25)}
    .hero-actions{flex-direction:column;align-items:stretch}
    .hero-actions .btn-lg{justify-content:center}
  }
</style>
